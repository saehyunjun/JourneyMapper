node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Drizzle / local SQLite
/data/*.db
/data/*.db-journal
/data/*.db-wal
/data/*.db-shm

# Phase 3 embedding corpus / vector cache
/data/embedding_corpus.jsonl
/data/embeddings_*.jsonl

# ClinicalTrials.gov raw envelopes — regenerable via scripts/fetch-clinicaltrials.mjs.
# Kept out of git because they exceed GitHub's per-file size limit (one is 130 MB).
**/clinical_trials/raw_studies.json
