<!--
	Placeholder for the journey-map landing route. +page.server.ts always
	throws a redirect, so this component never actually renders — the empty
	body is just here so SvelteKit treats the route as a real page route.
-->
